node_modules/
dist/
build/

.env
.env.local

infrastructure/judge0/judge0.conf

# Throwaway local test fixtures (e.g. curl -d @.scratch/login.json bodies) —
# these tend to carry real or throwaway credentials and shouldn't be committed.
.scratch/
login.json

.DS_Store
Thumbs.db

*.log

.claude/settings.local.json
