node_modules/
dist/
build/

.env
.env.local

infrastructure/judge0/judge0.conf

# Ad-hoc local curl request bodies (e.g. curl -d @login.json) — these tend to
# carry real or throwaway credentials and shouldn't be committed.
login.json

.DS_Store
Thumbs.db

*.log

.claude/settings.local.json
